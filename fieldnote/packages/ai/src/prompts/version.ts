/**
 * Prompt version.
 *
 * Stamped onto every generated value so a regression in field recall or edit
 * distance can be traced to the prompt change that caused it. Bump this on any
 * edit to the system prompt or the section template — including wording-only
 * edits, because those are exactly the ones that move the metrics without
 * anyone noticing.
 */
export const PROMPT_VERSION = '2026-07-25.1';
